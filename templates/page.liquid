{% form 'contact' %}
  {% if form.minimal %}
    {% layout none %}
  {% endif %}
{% endform %}

<div class="content-offset row">
  <div class="col-md-10 col-md-offset-1">
    <h1>{{ page.title }}</h1>
    <p>{{ page.content }}</p>
  </div>
</div>
