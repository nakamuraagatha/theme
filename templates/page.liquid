{% include 'l_setup' %}
{% include 'breadcrumb' %}

<div class="grid">

  <div class="grid__item large--two-thirds push--large--one-sixth">

    <h1>{{ page.title }}</h1>

    <div class="rte">
      {{ page.content }}
    </div>

  </div>

</div>
