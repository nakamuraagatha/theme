<div class="content-offset row">
  <div class="col-md-10 col-md-offset-1">
    {% form 'contact' %}
      <h1>{{ page.title }}</h1>
      <p>{{ page.content | replace: '[produkt]', form.product }}</p>

      {% if form.errors %}
        <div class="errorForm feedback">
          <p>Da ist etwas schief gelaufen! Bitte achte darauf, dass deine Email Adresse stimmt.</p>
        </div>
      {% endif %}

      {% if form.posted_successfully? %}
        <div class="successForm feedback">
          <p>Danke für dein Interesse. Wir benachrichtigen dich sobald du <strong>{{ form.product }}</strong> wieder bei Playmakers Football bestellen kannst.</p>
        </div>
      {% else %}
        <div class="form-group">
          <input type="hidden" name="contact[product]" value="Vorbestellung für: {{ form.product }}"/>
          <input type="hidden" name="contact[body]" value="{{ form.body }}"/>
        </div>
        <div class="form-group">
          <label>Dein Name:</label>
          <input type="text" id="contactFormName" name="contact[name]" value="{{ form.name }}"/>
        </div>
        <div class="form-group">
          <label>Deine Emailadresse:*</label>
          <input type="email" id="contactFormEmail" name="contact[email]" value="{{ form.email }}" required />
        </div>

        <div class="form-group">
          <br>
          <button type="submit" id="contactFormSubmit" class="btn btn-default">Verfügbarkeitsbenachrichtigung aktivieren</button>
        </div>
      {% endif %}
    {% endform %}
  </div>
</div>
